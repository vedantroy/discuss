diff --git a/node_modules/pdfjs-dist/build/pdf.js b/node_modules/pdfjs-dist/build/pdf.js
index 3e1de0b..7430335 100644
--- a/node_modules/pdfjs-dist/build/pdf.js
+++ b/node_modules/pdfjs-dist/build/pdf.js
@@ -4517,7 +4517,13 @@ if (_is_node.isNodeJS) {
 
   exports.NodeCanvasFactory = NodeCanvasFactory = class extends _base_factory.BaseCanvasFactory {
     _createCanvas(width, height) {
-      const Canvas = require("canvas");
+      // Cloudflare pages doesn't support canvas
+      // But that's ok b/c we never call this on the server
+      try {
+        const Canvas = require("canvas");
+      } catch (e) {
+          throw new e;
+      }
 
       return Canvas.createCanvas(width, height);
     }
